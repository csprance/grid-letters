import * as React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
`;

interface Props {}
const MiniGrid: React.FunctionComponent<Props> = ({}) => {
  return <Wrapper>MiniGrid</Wrapper>;
};

export default MiniGrid;
